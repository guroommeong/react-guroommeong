import React from 'react';
import { Layout } from '../../components/layout/screen';

const HomeScreen = () => {
  return (
    <Layout title={'메인'}>
      <p>홈화면 입니다.</p>
    </Layout>
  );
};

export default HomeScreen;
