// export interface getNotificationDataList {
//   length: number;
//   rvrs_no: number;
//   rnum: number;
//   total_cnt: number;
//   mntr_alrm_no: number;
//   reg_ymd_tm: string; // 날짜 시간 형식 문자열 (예: "20241025000020")
//   trsm_ttl_nm: string; // 제목
//   trsm_cn: string; // 내용
//   push_trgt_yn: 'Y' | 'N'; // 푸시 대상 여부 ("Y" 또는 "N")
//   rcd_drec_no: number | null; // 기록 번호 (없으면 null)
//   crtr_rcd_drec_no: number | null; // 기준 기록 번호 (없으면 null)
//   crtr_rcd_drec_nm: string | null; // 기준 기록 이름 (없으면 null)
//   crtr_rcd_drec_type_cd: string | null; // 기준 기록 타입 코드 (없으면 null)
// }
export {};
